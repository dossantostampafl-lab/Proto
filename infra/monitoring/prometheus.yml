global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/live-alerts.yml

scrape_configs:
  - job_name: proto-live-read-only
    metrics_path: /live/metrics/prometheus
    static_configs:
      - targets:
          - api:8000
